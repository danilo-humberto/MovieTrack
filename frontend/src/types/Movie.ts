export type Movie = {
  id?: string;
  title: string;
  description: string;
  releaseYear: number;
  gender: string;
  director: string;
  duration: number;
  imageUrl: string;
  trailerUrl?: string;
};
